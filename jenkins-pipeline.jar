/*
pipeline {
    agent { docker { image 'maven:3.3.3' } }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
*/


/*
// Multi-step pipeline (allowing to complete multiple steps along the way) 
//sh is used to execute a shell command in Pipeline
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
*/

/*
This code block includes powerful steps that wrap other steps which can easily solve problems like retrying (retry) steps until successful
or exiting if a steps takes too long. 
What this code block does is if we watned to retry our deployment 5 times, but never wanted to spend more than 3 minutes in total
before failing the stage. 
*/
pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                timeout(time: 3, unit: 'MINUTES') {
                    retry(5) {
                        sh './flakey-deploy.sh'
                    }
                }
            }
        }
    }
}







